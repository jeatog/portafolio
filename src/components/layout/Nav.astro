---
import { textos } from '@/utils/textos';
import ToggleTema from './ToggleTema.astro';
import ToggleIdioma from './ToggleIdioma.astro';

const elementosNav = [
  { titulo: textos.navegacion.experiencia, etiqueta: 'experiencia', url: '#experiencia', i18n: 'nav.experiencia' },
  { titulo: textos.navegacion.proyectos, etiqueta: 'proyectos', url: '#proyectos', i18n: 'nav.proyectos' },
  { titulo: textos.navegacion.educacion, etiqueta: 'educacion', url: '#educacion', i18n: 'nav.educacion' },
  { titulo: textos.navegacion.contacto, etiqueta: 'contacto', url: `mailto:${textos.sociales.correo}`, i18n: 'nav.contacto' },
];
---

<nav class="flex px-2 sm:px-3 text-xs sm:text-sm font-medium rounded-full text-[var(--color-subtle)] justify-center items-center">
  {elementosNav.map((enlace) => (
    <a
      class="relative block px-1.5 py-2 sm:px-2 transition hover:text-[var(--color-gold)]"
      data-seccion={enlace.etiqueta}
      data-i18n={enlace.i18n}
      href={enlace.url}
    >
      {enlace.titulo}
    </a>
  ))}
  <ToggleTema />
  <ToggleIdioma />
</nav>

<style>
  nav {
    animation: nav-fondo 1s linear both;
    animation-timeline: scroll();
    animation-range: 0 100px;
    background-color: var(--color-base);
  }

  @keyframes nav-fondo {
    0% {
      background-color: transparent;
    }
    to {
      background-color: var(--color-surface);
      box-shadow: 0 4px 30px rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(20px);
      border: 1px solid var(--color-highlight-med);
    }
  }
</style>
